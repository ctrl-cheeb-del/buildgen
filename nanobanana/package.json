{
  "name": "nanobanana",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "gen:textures": "bun run src/generate-textures.ts",
    "gen:textures:list": "bun run src/generate-textures.ts --list"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "replicate": "^1.0.0",
    "sharp": "^0.33.0"
  },
  "devDependencies": {
    "bun-types": "^1.2.0"
  }
}
